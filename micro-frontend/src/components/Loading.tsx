// @flow
import * as React from "react";
type Props = {};
export const Loading = (props: Props) => {
  return (
    <div
      style={{
        backgroundColor: "#202225",
        width: "100%",
        height: "100%",
        color: "#ffffff",
      }}
    >
      Carregando...
    </div>
  );
};
